<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sélection de Plans - WINABET.AI</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Outfit', sans-serif;
      background: #0a0a0a;
      color: #ffffff;
      min-height: 100vh;
      padding-bottom: 250px;
    }

    .container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 4rem 2rem;
    }

    .hero-section {
      text-align: center;
      margin-bottom: 4rem;
      padding-top: 2rem;
    }

    .hero-title {
      font-size: 4rem;
      font-weight: 300;
      margin-bottom: 1.5rem;
      color: #ffffff;
      line-height: 1.2;
    }

    .hero-subtitle {
      font-size: 1.5rem;
      color: #a1a1aa;
      font-weight: 300;
      max-width: 700px;
      margin: 0 auto 1rem;
      line-height: 1.6;
    }

    .trial-badge {
      display: inline-block;
      background: rgba(255, 255, 255, 0.1);
      border: 1px solid rgba(255, 255, 255, 0.2);
      padding: 0.75rem 2rem;
      border-radius: 9999px;
      font-size: 1.1rem;
      color: #ffffff;
      font-weight: 500;
      margin-top: 1rem;
    }

    .benefits-section {
      background: #1a1a1a;
      border: 1px solid #27272a;
      border-radius: 1.5rem;
      padding: 3rem;
      margin-bottom: 4rem;
    }

    .benefits-title {
      font-size: 2.5rem;
      font-weight: 300;
      text-align: center;
      margin-bottom: 3rem;
      color: #ffffff;
    }

    .benefits-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: 2.5rem;
    }

    .benefit-item {
      display: flex;
      align-items: start;
      gap: 1.25rem;
    }

    .benefit-icon {
      flex-shrink: 0;
      width: 32px;
      height: 32px;
      background: #27272a;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.1rem;
      color: #ffffff;
    }

    .benefit-content h3 {
      font-size: 1.3rem;
      margin-bottom: 0.75rem;
      font-weight: 500;
      color: #ffffff;
    }

    .benefit-content p {
      color: #a1a1aa;
      font-size: 1.05rem;
      line-height: 1.6;
    }

    .pricing-section {
      text-align: center;
      margin-bottom: 8rem;
    }

    .pricing-title {
      font-size: 2.5rem;
      font-weight: 300;
      margin-bottom: 1rem;
      color: #ffffff;
    }

    .pricing-subtitle {
      font-size: 1.1rem;
      color: #a1a1aa;
      margin-bottom: 3rem;
    }

    /* Sticky Footer */
    #sticky-plan-footer {
      position: fixed;
      bottom: 0;
      left: 0;
      right: 0;
      z-index: 1000;
      background: #0a0a0a;
      border-top: 1px solid #27272a;
      padding: 2rem;
      box-shadow: 0 -4px 20px rgba(0, 0, 0, 0.5);
    }

    .footer-content {
      max-width: 1400px;
      margin: 0 auto;
      display: flex;
      flex-direction: column;
      gap: 1.5rem;
    }

    .plans-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 1rem;
    }

    .plan-option {
      display: flex;
      align-items: center;
      gap: 1.25rem;
      padding: 1.75rem;
      background: #1a1a1a;
      border: 2px solid #27272a;
      border-radius: 1rem;
      cursor: pointer;
      transition: all 0.3s ease;
      position: relative;
    }

    .plan-option:hover {
      border-color: #3f3f46;
      background: #1f1f1f;
    }

    .plan-option.selected {
      border-color: #ffffff;
      background: #27272a;
    }

    .plan-option input[type="radio"] {
      width: 24px;
      height: 24px;
      cursor: pointer;
      accent-color: #ffffff;
      flex-shrink: 0;
    }

    .plan-info {
      flex: 1;
    }

    .plan-name {
      font-size: 1.3rem;
      font-weight: 500;
      margin-bottom: 0.5rem;
      color: #ffffff;
    }

    .plan-price {
      font-size: 1.8rem;
      font-weight: 600;
      color: #ffffff;
      margin-bottom: 0.5rem;
    }

    .plan-billing {
      font-size: 0.95rem;
      color: #a1a1aa;
    }

    .plan-badge {
      position: absolute;
      top: -0.75rem;
      right: 1.25rem;
      background: #ffffff;
      color: #0a0a0a;
      padding: 0.4rem 1rem;
      border-radius: 9999px;
      font-size: 0.75rem;
      font-weight: 700;
      letter-spacing: 0.05em;
    }

    .continue-button {
      background: #ffffff;
      color: #0a0a0a;
      border: none;
      padding: 1.5rem 3rem;
      border-radius: 1rem;
      font-size: 1.3rem;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.3s ease;
      font-family: 'Outfit', sans-serif;
      width: 100%;
      opacity: 1;
    }

    .continue-button:disabled {
      opacity: 0.4;
      cursor: not-allowed;
    }

    .continue-button:not(:disabled):hover {
      background: #f4f4f5;
      transform: translateY(-2px);
      box-shadow: 0 8px 20px rgba(255, 255, 255, 0.2);
    }

    /* Responsive */
    @media (max-width: 768px) {
      .hero-title {
        font-size: 2.5rem;
      }

      .hero-subtitle {
        font-size: 1.2rem;
      }

      .benefits-section {
        padding: 2rem;
      }

      .benefits-grid {
        grid-template-columns: 1fr;
        gap: 2rem;
      }

      .plans-grid {
        grid-template-columns: 1fr;
      }

      .plan-option {
        padding: 1.5rem;
      }

      .plan-name {
        font-size: 1.1rem;
      }

      .plan-price {
        font-size: 1.5rem;
      }

      .continue-button {
        font-size: 1.1rem;
        padding: 1.25rem 2rem;
      }

      body {
        padding-bottom: 500px;
      }

      #sticky-plan-footer {
        padding: 1.5rem 1rem;
      }
    }

    @media (min-width: 769px) and (max-width: 1024px) {
      .plans-grid {
        grid-template-columns: repeat(2, 1fr);
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <!-- Hero Section -->
    <div class="hero-section">
      <h1 class="hero-title">Rejoignez WINABET.AI</h1>
      <p class="hero-subtitle">
        Maximisez vos gains avec notre IA de paris sportifs. Analyses en temps réel, recommandations d'experts, et résultats prouvés.
      </p>
      <div class="trial-badge">
        🎁 1 jour d'essai gratuit inclus
      </div>
    </div>

    <!-- Benefits Section -->
    <div class="benefits-section">
      <h2 class="benefits-title">Tout ce dont vous avez besoin pour gagner</h2>
      <div class="benefits-grid">
        <div class="benefit-item">
          <div class="benefit-icon">✓</div>
          <div class="benefit-content">
            <h3>Accès complet aux bets du jour</h3>
            <p>Recevez nos meilleures recommandations quotidiennes analysées par notre IA avancée avec plus de 2800+ variables</p>
          </div>
        </div>

        <div class="benefit-item">
          <div class="benefit-icon">✓</div>
          <div class="benefit-content">
            <h3>Parlays à haute probabilité</h3>
            <p>Maximisez vos gains avec nos combinaisons optimisées qui augmentent vos chances de succès</p>
          </div>
        </div>

        <div class="benefit-item">
          <div class="benefit-icon">✓</div>
          <div class="benefit-content">
            <h3>Historique complet</h3>
            <p>Suivez toutes vos performances et statistiques détaillées pour améliorer vos stratégies</p>
          </div>
        </div>

        <div class="benefit-item">
          <div class="benefit-icon">✓</div>
          <div class="benefit-content">
            <h3>Support prioritaire</h3>
            <p>Bénéficiez d'une assistance rapide et dédiée via WhatsApp pour toutes vos questions</p>
          </div>
        </div>

        <div class="benefit-item">
          <div class="benefit-icon">✓</div>
          <div class="benefit-content">
            <h3>Mises à jour IA en temps réel</h3>
            <p>Profitez des dernières analyses de notre système IA qui s'améliore continuellement</p>
          </div>
        </div>

        <div class="benefit-item">
          <div class="benefit-icon">✓</div>
          <div class="benefit-content">
            <h3>Analyses détaillées</h3>
            <p>Comprenez chaque recommandation avec nos analyses complètes et transparentes</p>
          </div>
        </div>
      </div>
    </div>

    <!-- Pricing Info -->
    <div class="pricing-section">
      <h2 class="pricing-title">Choisissez votre fréquence de paiement</h2>
      <p class="pricing-subtitle">Plus vous vous engagez longtemps, plus vous économisez</p>
    </div>
  </div>

  <!-- Sticky Footer with Payment Options -->
  <div id="sticky-plan-footer">
    <div class="footer-content">
      <div class="plans-grid">
        
        <!-- Monthly Plan -->
        <label class="plan-option" data-plan="monthly">
          <input type="radio" name="payment-frequency" value="monthly">
          <div class="plan-info">
            <div class="plan-name">Mensuel</div>
            <div class="plan-price">99$ / mois</div>
            <div class="plan-billing">Facturé 99$ chaque mois</div>
          </div>
        </label>

        <!-- Quarterly Plan -->
        <label class="plan-option" data-plan="quarterly">
          <input type="radio" name="payment-frequency" value="quarterly">
          <div class="plan-info">
            <div class="plan-name">Trimestriel</div>
            <div class="plan-price">79.99$ / mois</div>
            <div class="plan-billing">Facturé 239.97$ tous les 3 mois</div>
          </div>
        </label>

        <!-- Bi-annually Plan (RECOMMENDED) -->
        <label class="plan-option" data-plan="biannually">
          <div class="plan-badge">RECOMMANDÉ</div>
          <input type="radio" name="payment-frequency" value="biannually">
          <div class="plan-info">
            <div class="plan-name">Semestriel</div>
            <div class="plan-price">64.99$ / mois</div>
            <div class="plan-billing">Facturé 389.94$ tous les 6 mois</div>
          </div>
        </label>

        <!-- Annually Plan (BEST VALUE) -->
        <label class="plan-option" data-plan="annually">
          <div class="plan-badge">MEILLEURE VALEUR</div>
          <input type="radio" name="payment-frequency" value="annually">
          <div class="plan-info">
            <div class="plan-name">Annuel</div>
            <div class="plan-price">49.99$ / mois</div>
            <div class="plan-billing">Facturé 599.88$ / an</div>
          </div>
        </label>

      </div>

      <button id="continue-to-checkout" class="continue-button" disabled>
        Continuer vers le paiement
      </button>
    </div>
  </div>

  <script>
    let selectedPlan = null;

    // Plan data with links (to be filled later)
    const plans = {
      monthly: {
        name: 'Mensuel',
        price: '99$',
        billing: 'Facturé 99$ chaque mois',
        link: '' // To be filled
      },
      quarterly: {
        name: 'Trimestriel',
        price: '79.99$',
        billing: 'Facturé 239.97$ tous les 3 mois',
        link: '' // To be filled
      },
      biannually: {
        name: 'Semestriel',
        price: '64.99$',
        billing: 'Facturé 389.94$ tous les 6 mois',
        link: '' // To be filled
      },
      annually: {
        name: 'Annuel',
        price: '49.99$',
        billing: 'Facturé 599.88$ / an',
        link: '' // To be filled
      }
    };

    function selectPlan(planKey) {
      selectedPlan = planKey;
      
      // Update UI
      document.querySelectorAll('.plan-option').forEach(option => {
        const optionPlanKey = option.dataset.plan;
        const radio = option.querySelector('input[type="radio"]');
        
        if (optionPlanKey === planKey) {
          option.classList.add('selected');
          if (radio) radio.checked = true;
        } else {
          option.classList.remove('selected');
          if (radio) radio.checked = false;
        }
      });
      
      // Enable button
      const button = document.getElementById('continue-to-checkout');
      if (button) {
        button.disabled = false;
      }
    }

    function continueToCheckout() {
      if (!selectedPlan) return;
      
      const plan = plans[selectedPlan];
      if (plan && plan.link) {
        window.location.href = plan.link;
      } else {
        alert('Lien de paiement non configuré. Veuillez contacter le support.');
      }
    }

    // Event listeners
    document.addEventListener('DOMContentLoaded', function() {
      // Plan option click handlers
      const planOptions = document.querySelectorAll('.plan-option');
      planOptions.forEach(option => {
        option.addEventListener('click', function() {
          const planKey = this.dataset.plan;
          selectPlan(planKey);
        });
      });

      // Continue button click handler
      const continueButton = document.getElementById('continue-to-checkout');
      if (continueButton) {
        continueButton.addEventListener('click', continueToCheckout);
      }
    });
  </script>
</body>
</html>

{% schema %}
{
  "name": "Page Sélection Plans",
  "settings": []
}
{% endschema %}
